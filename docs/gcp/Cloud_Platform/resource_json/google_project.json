{
  "resource_name": "google_project",
  "subcategory": "Cloud Platform",
  "arguments": {
    "name": {
      "description": "The display name of the project.",
      "required": true,
      "security_impact": false,
      "rationale": "Label only and does not affect security",
      "compliant": null,
      "non-compliant": null,
      "parent": null
    },
    "project_id": {
      "description": "The project ID. Changing this forces a new project to be created.",
      "required": true,
      "security_impact": true,
      "rationale": "To ensure consistency and security, naming rules must be followed.  Non-standard IDs may violate the enforcement of policies.",
      "compliant": "project_id = \"proj-app-dev\"",
      "non-compliant": "project_id = \"myrandomproject\"",
      "parent": null
    },
    "org_id": {
      "description": "Changing this forces a new project to be created.  Only one of `org_id` or `folder_id` may be specified. If the `org_id` is specified then the project is created at the top level. Changing this forces the project to be migrated to the newly specified organization.",
      "required": false,
      "security_impact": true,
      "rationale": "The project needs to be a part of a folder or an organisation. If both are missing, the project is considered discarded and harder to manage",
      "compliant": "org_id = \"1234567\"",
      "non-compliant": "org_id = null and folder_id = null",
      "parent": null
    },
    "folder_id": {
      "description": "created under. Only one of `org_id` or `folder_id` may be specified. If the `folder_id` is specified, then the project is created under the specified folder. Changing this forces the project to be migrated to the newly specified folder.",
      "required": false,
      "security_impact": true,
      "rationale": "The project must be associated to an organisation or folder for administration.",
      "compliant": "folder_id = \"9876543\"",
      "non-compliant": "org_id = null and folder_id = null",
      "parent": null
    },
    "billing_account": {
      "description": "belongs to. The user or service account performing this operation with Terraform must have at minimum Billing Account User privileges (`roles/billing.user`) on the billing account. See [Google Cloud Billing API Access Control](https://cloud.google.com/billing/docs/how-to/billing-access) for more details.",
      "required": false,
      "security_impact": true,
      "rationale": "Projects without a billing account cannot run services or be monitored for costs",
      "compliant": "billing_account = \"000000-111111-222222\"",
      "non-compliant": "billing_account = null",
      "parent": null
    },
    "labels": {
      "description": "**Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "security_impact": true,
      "rationale": "Labels identify the owning team or purpose of the project. Without them, it is unclear who is responsible",
      "compliant": "labels = { team = \"security\" }",
      "non-compliant": "labels = {}",
      "parent": null
    },
    "terraform_labels": {
      "description": "The combination of labels configured directly on the resource and default labels configured on the provider.",
      "required": false,
      "security_impact": false,
      "rationale": "Metadata only and not security-enforcing",
      "compliant": null,
      "non-compliant": null,
      "parent": null
    },
    "effective_labels": {
      "description": "All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services.",
      "required": false,
      "security_impact": false,
      "rationale": "Observation field only and has no direct security impact",
      "compliant": null,
      "non-compliant": null,
      "parent": null
    },
    "auto_create_network": {
      "description": "to `true`, where it is created. If set to `false`, the default network will still be created by GCP but will be deleted immediately by Terraform. Therefore, for quota purposes, you will still need to have 1 network slot available to create the project successfully, even if you set `auto_create_network` to `false`. Note that when `false`, Terraform enables `compute.googleapis.com` on the project to interact with the GCE API and currently leaves it enabled.",
      "required": false,
      "security_impact": true,
      "rationale": "Default networks are insecure and should not be created",
      "compliant": "auto_create_network = false",
      "non-compliant": "auto_create_network = true",
      "parent": null
    },
    "deletion_policy": {
      "description": "against any destroy actions caused by a terraform apply or terraform destroy. Setting ABANDON allows the resource to be abandoned rather than deleted, i.e., the Terraform resource can be deleted without deleting the Project via the Google API. Possible values are: \"PREVENT\", \"ABANDON\", \"DELETE\". Default value is `PREVENT`.",
      "required": false,
      "security_impact": true,
      "rationale": "Deletion protection prevents accidental loss of resources and data",
      "compliant": "deletion_policy = \"PREVENT\"",
      "non-compliant": "deletion_policy = \"DELETE\"",
      "parent": null
    },
    "tags": {
      "description": "A map of resource manager tags. Resource manager tag keys and values have the same definition as resource manager tags. Keys must be in the format tagKeys/{tag_key_id}, and values are in the format tagValues/456. The field is ignored when empty. The field is immutable and causes resource replacement when mutated. This field is only set at create time and modifying this field after creation will trigger recreation. To apply tags to an existing resource, see the google_tags_tag_value resource.",
      "required": false,
      "security_impact": true,
      "rationale": "Tags such as environment (dev, test, prod) are required for compliance and cost management",
      "compliant": "tags = { \"tagKeys/env\" = \"tagValues/prod\" }",
      "non-compliant": "tags = {}",
      "parent": null
    }
  }
}
